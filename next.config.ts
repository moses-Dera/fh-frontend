import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // allowedDevOrigins is experimental in some versions, or strictly for next.config.js? 
  // keeping it as is but in correct structure if supported, otherwise commenting out to be safe for now 
  // as it was likely copy-pasted wrong.
  // Using empty config for stability first.
};

export default nextConfig;